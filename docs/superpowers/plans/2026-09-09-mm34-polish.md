# MM34 landing page polish — implementation plan

> **For agentic workers:** Execute inline in this session (executing-plans). Steps use checkbox syntax.

**Goal:** Polish `Methods 3-4 Landing Page.dc.html` so it reads as one continuous, evenly styled, calmly animated story, with the three pillars merged into one "Contour System" flow, then repack it into a single-file HTML for Angad.

**Architecture:** One HTML source (dc-runtime templating: `sc-for`, `sc-if`, `{{ }}` bound to a `DCLogic` component in the trailing `<script type="text/x-dc">`). CSS lives in the `<style>` block at lines 1212–1303. Page logic (state, `renderVals`) lives in the trailing script. A new `tools/pack.py` inlines everything into `Contour Methods 3-4 Landing Page.html`. A new `tools/render.sh` screenshots the page with the Playwright Chromium already on this Mac.

**Tech Stack:** Vanilla HTML/CSS, dc-runtime (`support.js`), React via `_ds_bundle.js`, Leaflet 1.9.4, Python 3 (stdlib only) for packing, Chromium headless for QA.

**Spec:** `docs/superpowers/specs/2026-09-09-mm34-polish-design.md`

## Global Constraints
- Do not change copy. Only structural/visual/motion edits. Exception: rail label "Curriculum" → "Tutoring", campus dropdown list.
- Prototype control bar stays.
- Text colour only cream / charcoal / navy. Accents (OG blue, highlighter) as fills only, never both in one component.
- All motion respects `prefers-reduced-motion: reduce`.
- `standalone-src.dc.html` is never edited.
- Work in `Methods 3-4 Landing Page.dc.html`. Take a backup copy first: `scraps/backup-2026-09-09-methods.dc.html`.

---

### Task 0: Tooling (render + pack)
**Files:** Create `tools/render.sh`, `tools/pack.py`.
- [ ] `tools/render.sh <out.png> [width] [height] [url-path]` starts `python3 -m http.server 8765` if not running, screenshots with Chromium at `--force-device-scale-factor=0.5` and `--window-size=W,H`.
- [ ] `tools/pack.py` reads the dc file, inlines: `support.js`, `image-slot.js`, `_ds/.../tokens/*.css`, `_ds/.../styles.css`, `_ds_bundle.js`, Leaflet JS from a cached copy in `tools/vendor/leaflet.js` (download once), every `src="assets/..."`, `href="assets/..."`, `url(assets/...)`, and string literals `"assets/..."` inside the x-dc script, as base64 data URIs. Writes `Contour Methods 3-4 Landing Page.html`. Prints size.
- [ ] Verify: pack current file, open packed file from `file://` in Chromium, screenshot, confirm images visible and no `[bundle]` errors.

### Task 1: Motion + layout tokens (CSS)
**Files:** Modify `Methods 3-4 Landing Page.dc.html` `<style>` block (1212–1303).
- [ ] Add tokens to `:root`: `--ease:cubic-bezier(.22,1,.36,1); --dur-s:.35s; --dur-m:.6s; --dur-l:.9s; --sec-pad:clamp(72px,9vw,112px)`.
- [ ] Add reveal system:
  ```css
  [data-reveal]{opacity:0;transform:translateY(18px);transition:opacity var(--dur-m) var(--ease),transform var(--dur-m) var(--ease);transition-delay:calc(var(--i,0)*80ms)}
  [data-reveal="scale"]{transform:scale(.96)}
  [data-reveal="left"]{transform:translateX(-18px)}
  [data-reveal].in{opacity:1;transform:none}
  ```
  plus `@media (prefers-reduced-motion:reduce){[data-reveal]{opacity:1;transform:none;transition:none}}`.
- [ ] Unified section rhythm: `.sec{padding:var(--sec-pad) 0!important}` and remove per-section `border-top` reliance by adding `.sec + .sec{border-top:1px solid var(--border-subtle)}`; navy sections opt out with `.sec.dark + .sec, .sec + .sec.dark{border-top:none}`.
- [ ] Card hover system: `.card-h{transition:transform var(--dur-s) var(--ease),box-shadow var(--dur-s) var(--ease)} .card-h:hover{transform:translateY(-3px);box-shadow:var(--shadow-md)}`.
- [ ] Side rail: frosted cream bg already present; add progress line `#secrail::before{content:"";position:absolute;left:50%;top:14px;bottom:14px;width:2px;background:var(--border-subtle);transform:translateX(-50%)}` and `#secrail .prog{position:absolute;left:50%;top:14px;width:2px;background:var(--navy);transform:translateX(-50%);height:calc(var(--sp,0)*(100% - 28px));transition:height .2s linear}`. Hide on the bottom-bar layout (`max-width:1250px`).
- [ ] Verify with render: page still looks identical apart from spacing.

### Task 2: Reveal wiring (JS)
**Files:** Modify trailing x-dc script `_wireDom()`.
- [ ] Replace the `section h2` animate() observer with a generic observer over `[data-reveal]` that adds class `in` (threshold .18, rootMargin `0 0 -6%`). Stagger: for `[data-stagger]` parents, set `--i` on children and add `data-reveal` to each child if absent.
- [ ] In `_armGlobalSpy` compute `--sp` (scroll progress 0..1 across document) and set on `#secrail`; also set `data-ids` active as today.
- [ ] Auto-tag: at wire time, add `data-reveal` to every `section .ctrp > h2`, `.ctrp > p`, `.g2>*`, `.g3>*`, `.g4>*`, `.offcol`, stat tiles `[data-cnt]` parents, tutor cards, campus cards, FAQ groups. Markup untouched, tagging is JS-side so templating re-renders keep working (re-run tagging on `componentDidUpdate` throttled).
- [ ] Verify with render at 1440: reveals visible when forcing `.in` on all (headless has no scroll). Add `?reveal=all` URL flag that marks all `.in` for QA screenshots.

### Task 3: Unified Contour System section
**Files:** Modify markup lines ~1830–2311 and x-dc script (`state`, `_pillarGo`, `_tabMap`, `renderVals` pillar block, spy ids).
- [ ] Replace the three `.pwrap > .pstrip + section#pillar-N` blocks with one `<section id="system" class="sec">` containing:
  - header: eyebrow chip "The Contour System · 19 parts", h2 "One system. Nineteen parts. One weekly fee." is copy → NOT allowed. Reuse existing pillar h2 text as sub-headings instead: keep `#method` h2 as the section intro (unchanged) and drop the three big pillar h2s into the rail as group headings (text preserved: "Expert Tutoring", "Comprehensive Resources", "Personalised Support" + their lead paragraphs as group descriptions).
  - `.pillarg` grid: left `.jsticky` rail (now `position:sticky; top:132px`) with three groups, each: coloured group chip (navy / og-blue / highlighter) + `<sc-for list="{{ sysSteps1 }}">` etc. The step button template is the existing one.
  - right column: one panel wrapper. Inside, the three existing `.tabpanel` blocks each wrapped in `<sc-if value="{{ sysP1 }}">`, `{{ sysP2 }}`, `{{ sysP3 }}`. Their inner content is moved verbatim.
  - anchors: `<span id="pillar-1"></span>` etc. placed on group headings so rail dots + `_tabMap` still resolve.
  - after the grid: the existing "Your program" (Core/Mastery/Extension) block, moved out of p1 panel so it always shows.
- [ ] Script: `state.sysP = 1`. `_pillarGo(key,i,scroll)` also sets `sysP: Number(key[1])` and scrolls to `#system` panel top (not pillar). `renderVals` adds `sysP1/2/3` booleans, `sysSteps1/2/3` = existing `pXsteps` (rename or alias), prev/next wrap across pillars (`p1` idx 5 next → `p2` idx 0). Panel chip colour bound via existing per-pillar markup.
- [ ] Spy ids list: replace `pillar-1..3` handling with `system`; rail dots: one dot "System" → `#system` with `data-ids="system,pillar-1,pillar-2,pillar-3,method"`; remove the three separate pillar dots; keep total dots ≤ 9. Rail label fix: none needed after merge (no "Curriculum" label).
- [ ] Remove `.pstrip` CSS/markup and the `__mm34Drive` pstrip block (dead after merge).
- [ ] Verify render: rail lists 19 steps in 3 groups; panel shows step 01; clicking (simulate via `?step=14` URL flag handled in componentDidMount → `_pillarGo("p3",0)`) shows the Support panel.

### Task 4: Story thread + even headers
**Files:** markup of each section header (`#story`, `#results`, `#reviews`, `#tutors`, `#academy`, `#method`, `#partnership`, `#availability`, `#people`, `#offer`, `#founders-note`, `#price`, `#final`, `#faq`).
- [ ] Introduce `.sechead` structure: `<div class="sechead"><span class="eyebrow">…</span><h2>…</h2><p class="lead">…</p></div>` with CSS `.eyebrow{display:inline-flex;align-items:center;gap:8px;font:600 12.5px/1 var(--font-body);color:var(--navy);background:var(--surface-raised);border:1px solid var(--border-subtle);border-radius:999px;padding:8px 13px;margin-bottom:16px} .eyebrow::before{content:"";width:8px;height:8px;border-radius:2px;background:var(--navy)} .sechead h2{font-family:var(--font-display);font-weight:700;font-size:clamp(32px,3.6vw,44px);letter-spacing:-0.02em;line-height:1.12;color:var(--navy);margin:0 0 14px} .sechead .lead{font-size:17px;line-height:1.6;color:var(--text-muted);margin:0 0 40px;max-width:56ch} .dark .sechead h2{color:var(--cream)} .dark .eyebrow{color:var(--cream);background:rgba(255,249,241,.08);border-color:var(--border-on-navy)}`.
- [ ] Existing eyebrow text is kept where present (e.g. "Letter from co-founders", "Why Contour exists" chip in story). Where a section has no eyebrow, use its `data-screen-label` noun (already on the section: "Results", "Reviews", "Your tutors", "Locations", "Pricing", "FAQ"). No new copy invented beyond these labels.
- [ ] Connective thread: `.sec .ctrp{position:relative} .sec.thread::before{content:"";position:absolute;left:clamp(32px,11vw,210px);top:calc(-1*var(--sec-pad));height:var(--sec-pad);width:2px;background:linear-gradient(var(--border-subtle),var(--navy));opacity:.55}` applied to every `.sec` after `#story`. Hidden under 880px.
- [ ] Verify render: headers align on one left edge, identical eyebrow style everywhere.

### Task 5: Even components
- [ ] Results: wrap leaderboard + stat tiles in one `.g2` with `align-items:stretch`; stat tiles get `.card-h`.
- [ ] Reviews cards, tutor cards, campus cards, offer columns, FAQ items: add `.card-h`; set consistent `border-radius:var(--radius-xl)` where they currently use `--radius-lg`/`2xl` mix (keep `2xl` only for the hero and pillar panel).
- [ ] CTA rows: every `Book your free trial` + reassurance line uses the same flex row (`.ctarow{display:flex;align-items:center;gap:14px;flex-wrap:wrap}`).
- [ ] Journey thumbnails: replace repeated `opt/mmCampus2.jpg`/building shots in `defs` with distinct existing files: use `opt/mmJourney3.jpg`, `opt/mmJourney6.jpg`, `ramodh/week-15.jpg`, `ramodh/pillar-1.jpg`, `ramodh/pillar-2.jpg`, `ramodh/pillar-3.jpg`, `opt/328d3f6e.jpg`, `opt/c40515b3.jpg`, `opt/f31a3dce.jpg`, `opt/dbdd6052.jpg`, `opt/4d08b68a.jpg`, `opt/e52d2f1e.jpg`, `opt/6b3cc21d.jpg`, `opt/c473f05f.jpg`, `opt/e54782f9.jpg`, `opt/df97af1f.jpg`, `opt/5edb3b2e.jpg`, `opt/ef9de5bf.jpg`. Campus cards rotate through `opt/mmCampus1..8` (identical today) plus the four `opt/*` campus-ish photos so cards visually differ.
- [ ] Campus dropdown: `campusOptions` → `["Glen Waverley", "Box Hill", "Melbourne CBD", "Point Cook", "Bentleigh", "Springvale", "Narre Warren", "Online"]`.
- [ ] FAQ rail dot → `href="#faq" data-ids="faq"`; add "Trial" dot `href="#final" data-ids="final"` only if dot count ≤ 9.

### Task 6: QA + pack
- [ ] Render 1440 full page (`?reveal=all`), inspect every section tile.
- [ ] Render 390 width, inspect hero, system section, price, FAQ.
- [ ] Console: run Chromium with `--enable-logging=stderr --v=0` and grep for `Uncaught`.
- [ ] `python3 tools/pack.py`, open packed file via `file://`, render, confirm.
- [ ] Update memory `mm34-open-issues-2026-09.md` (mark fixed items).
